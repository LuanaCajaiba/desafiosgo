package main

import (
	"fmt"
)

func main() {
	t := ("verificando os caracteres")
	//strconv.Itoa
	/*fmt.Println("Tamanho matematicamente calculado:", math.Floor(math.Log10(math.Abs((57890))))+1)
	for i := 0; i < len(t); i++ {
		fmt.Printf("%c\n", t[i])
	}*/
	fmt.Println(len(t))
}
